name: 'car_sales_dbt'
version: '1.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target"]

models:
  car_sales_dbt:
    +schema: "public"
    +materialized: view